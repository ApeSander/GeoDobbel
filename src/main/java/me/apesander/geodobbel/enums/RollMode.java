package me.apesander.geodobbel.enums;

public enum RollMode {
    ADD, // Adds all dice
    AVERAGE, // Average of all dice
    HIGHEST, // Highest of the dice
    LOWEST, // Lowest of the dice
    NONE // Rolls turned off (invisible ADD if ScoreType is not NONE)
}
