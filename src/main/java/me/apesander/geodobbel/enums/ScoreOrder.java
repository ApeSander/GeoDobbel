package me.apesander.geodobbel.enums;

public enum ScoreOrder {
    ASCENDING, // Sorts players in ascending order
    DESCENDING, // Sorts players in descending order
    NONE // Does not sort player
}
