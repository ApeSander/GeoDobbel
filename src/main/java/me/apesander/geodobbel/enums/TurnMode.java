package me.apesander.geodobbel.enums;

public enum TurnMode {
    NORMAL, // Turns go 'clockwise'
    PINGPONG, // Turns go 'clockwise' and then 'counterclockwise'
    RANDOM, // Turns are random
    MANUAL, // Turns wont go automatically
    OFF // Turns are off
}
