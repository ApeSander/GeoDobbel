package me.apesander.geodobbel.models;

import java.util.UUID;

// This object is a dice spectator
public class DiceSpectator extends DiceUser {
    public DiceSpectator(UUID uuid) {
        super(uuid);
    }
}
